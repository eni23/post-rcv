## usage

### server:
- `npm install`
- configure `config/config.yaml` to your needs.
- setup public-url
- `npm start`

### client
- `curl -s public-url > upload.sh`
- `chmod +x upload.sh`
- `./upload.sh /path/to/file`
